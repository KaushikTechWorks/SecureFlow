# Production Environment Variables
# Note: These are managed by CloudFormation in production

# Architecture: CloudFront + S3 + Lambda + API Gateway + PostgreSQL
# Database Configuration (auto-configured by CloudFormation)
DATABASE_URL=postgresql://postgres:SecureFlow2024!@your-rds-endpoint:5432/secureflow

# API Configuration (set during frontend build)
REACT_APP_API_URL=https://your-api-id.execute-api.us-east-1.amazonaws.com/prod

# AWS Configuration
AWS_REGION=us-east-1
ENVIRONMENT=prod

# Stack Configuration
STACK_NAME=secureflow-production

# Note: Actual values are injected during deployment by the deployment script
